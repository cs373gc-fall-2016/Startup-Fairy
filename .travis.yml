sudo: required
dist: trusty

language: python

services: 
  docker

python:
 - "3.5"

before_install:
  - pip install --upgrade pip 

install:
  - pip install autopep8
  - pip install coverage
  - pip install numpy
  - pip install pylint

before_script:
  - uname -a
  - printenv | sort
  - make versions

script:
  - docker-compose up -d
  - make test

notifications:
  email: false
